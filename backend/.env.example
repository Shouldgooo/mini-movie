# Copy this file to `.env` and replace the placeholders with real local values.
# Connection string for PostgreSQL.
# Requires PostgreSQL >= 15.

DATABASE_URL="postgresql://user:password@localhost:5432/minimovie"
JWT_SECRET="replace-with-a-long-random-string"
CORS_ORIGIN="http://localhost:3000"
PORT=4000
TMDB_ACCESS_TOKEN=
